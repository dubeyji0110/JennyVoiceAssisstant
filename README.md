### JENNY your personal voice assisstant
`pip install SpeechRecognition`
`pip install pyttsx3`
`pip install pywhatkit`
`pip install wikipedia`
`pip install pyjokes`
`pip install pipwin`
`pipwin install PyAudio`
